# UpdateLog v0.10.1

## Metadata
- Category: main
- Scope: Main
- Version: 0.10.1
- Date: 2026-03-21
- Branch: main
- Commit: 53c1a1e

## Summary
- 調整啟動入口為核心流程啟動，改採 llm+discord+talk-engine 順序上線

## Changes
### Added
- 新增 CLI 參數 --llm-base-url、--llm-model、--talk-relay-enabled、--talk-relay-error-reply
- 新增 ENV fallback LLM_REMOTE_BASE_URL、LLM_REMOTE_MODEL、TALK_RELAY_ENABLED、TALK_RELAY_ERROR_REPLY

### Changed
- src/index.ts 預設啟動由 onlineAll 改為依序啟動 system:llm-remote-gateway -> system:discord -> system:talk-engine
- 保留 --plugin 單插件啟動路徑並維持舊參數 --url 相容

### Fixed
- 當 llm baseUrl 缺失時改為 fail-fast，避免半啟動狀態
- 核心流程中途啟動失敗時會執行 offlineAll 清理已啟動插件

### Removed
- N/A

## Impact
- 預設啟動行為對齊目前對話整套流程，不再啟動非核心插件
- 部署可透過 CLI/ENV 明確控制 LLM 與 talk relay 啟動參數

## Tests
- 更新 tests/pluginsManager/index.integration.test.ts：驗證核心三插件依序啟動、缺少 LLM_REMOTE_BASE_URL fail-fast、--plugin 相容路徑、CLI/ENV 解析
- 執行 yarn test 與 yarn build 通過

## Risks & Rollback
- 若既有部署依賴 onlineAll 全開行為，升級後需改為顯式指定 --plugin 或分開啟動
- TALK_RELAY_ENABLED 若填入非法值會在啟動前直接報錯

## Notes
- 主流程仍遵循 docs 的 discoverPlugins -> validateDependencies -> online/offline lifecycle 契約
- 本次僅修改啟動腳本與整合測試，不新增其他文件
