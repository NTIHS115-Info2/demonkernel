# UpdateLog v0.18.0

## Metadata
- Category: main
- Scope: Main
- Version: 0.18.0
- Date: 2026-05-22
- Branch: main
- Commit: 6396bc2

## Summary
- 完成 README 版本同步、agent 更新流程規範與 focused test coverage

## Changes
### Added
- 新增根目錄 README 入口內容，包含固定 DEMONKERNEL_VERSION marker、專案定位、核心架構、system plugin 導覽與常用維護命令
- 新增 `tools/updatelog/lib/readme-version.js`，提供最新 Main UpdateLog 版本掃描、README marker 讀取、同步與檢查 helper
- 新增 `sync-readme-version` 與 `check-readme-version` CLI 子命令，以及 `yarn readme:sync-version`、`yarn readme:check-version` package scripts
- 新增 `tools/updatelog/__tests__/readme-version.test.js`，使用 temp directory fixtures 覆蓋 latest version scan、README sync、idempotent sync 與 check failure cases

### Changed
- 更新 `docs/updatelog-spec.md` 與 `AGENTS.md`，明確說明 agent 更新後的 UpdateLog、README sync/check、README version marker 與 final report validation 規則
- 確認 README 顯示版本來源為 `Updates/Main` 最新 Main UpdateLog；`package.json` version 也是更新目標，但第一版 README 同步工具不自動修改 `package.json`

### Fixed
- 補齊 README version workflow 的 focused tests，確認缺 marker、重複 marker 與 README 版本落後時會清楚失敗

### Removed
- 本次未移除既有功能或檔案

## Impact
- 後續 agent 可用一致的 UpdateLog 與 README version workflow 驗證版本顯示，降低只修改程式碼但遺漏 README、UpdateLog 或 final validation 的風險

## Tests
- `yarn test tools/updatelog` 通過，6 個 test files、20 個 tests 全部通過
- `yarn readme:sync-version` 通過，README previous/latest 皆為 `v0.18.0`，`changed: no`
- `yarn readme:check-version` 通過，README `v0.18.0` 等於 latest Main UpdateLog `v0.18.0`
- `yarn updatelog:validate:staged` 通過
- Direct UpdateLog validation 通過：`Updates/Main/v0/v0.18/v0.18.0.md`
- `yarn lint` 通過且無 error；仍有既有 warning：`tests/systemPlugins/discord.test.ts:379` 的 `_content` 未使用

## Risks & Rollback
- README sync/check 只替換唯一的 DEMONKERNEL_VERSION marker；若 marker 缺失、重複或格式異常，工具會失敗而不猜測修改位置
- 若此 workflow 需要回復，可移除 README version scripts、CLI 子命令與 helper，並把 README marker 改回手動維護；UpdateLog 與 ExecPlan 已保留本次驗證證據

## Notes
- Target Version: v0.18.0；Milestone 6 未修改 `README.md`、`AGENTS.md`、`package.json` 或 `docs`，僅新增 focused tests 並更新本 Main UpdateLog 與 active ExecPlan living sections
