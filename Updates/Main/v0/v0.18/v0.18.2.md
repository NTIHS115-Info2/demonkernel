# UpdateLog v0.18.2

## Metadata
- Category: main
- Scope: Main
- Version: 0.18.2
- Date: 2026-05-22
- Branch: main
- Commit: 6396bc2

## Summary
- 重寫 README 為人類導向專案門面

## Changes
### Added
- 補充 README 的專案定位、核心能力與使用情境說明

### Changed
- 將英文與繁體中文 README 從 agent 操作導向改為人類可讀的專案介紹
- 明確將 `package.json` 的 `version` 納入本次版本同步目標，與最新 Main UpdateLog `v0.18.2` 對齊

### Fixed
- 降低 README 與 AGENTS.md 的角色重疊，讓 README 聚焦說明 Demonkernel 是什麼與如何開始
- 補強雙語 README consistency check 的驗證描述，列出實際檢查的同步面向

### Removed
- 本版未移除既有功能

## Impact
- 新讀者可從 README 快速理解 Demonkernel 的目的、架構、內建能力與啟動方式

## Tests
- `yarn readme:check-version` 通過，README marker 與最新 Main UpdateLog 皆為 `v0.18.2`
- `yarn updatelog:validate:staged` 通過
- 已執行雙語 README consistency check，確認 `README.md` 與 `README.zh-TW.md` 雙向連結存在、章節順序等價、repository facts 對應、command lists 一致、release marker 皆為 `v0.18.2`，且維護規範入口皆指向 `AGENTS.md`、`.agent/PLANS.md` 與 `Updates`
- 已確認 `package.json` 的 `version` 為 `0.18.2`，與最新 Main UpdateLog `v0.18.2` 的 semantic version 對齊；目前 README sync/check 工具不自動驗證 package version

## Risks & Rollback
- README 仍需與實際 source/config 保持一致；若未同步繁中版可能造成雙語資訊漂移
- `package.json` version 目前靠 agent 手動對齊 Main UpdateLog；若後續需要更嚴格保證，可新增 package-version checker 或整合到 existing validation

## Notes
- Target Version: v0.18.2 patch for readme-version-agent-workflow.exec
- `yarn readme:sync-version` 已將 `README.md` 從 `v0.18.1` 同步到 `v0.18.2`；`README.zh-TW.md` 已手動同步 marker 與等價內容
- 本 patch 的版本來源以最新 Main UpdateLog 為準；`README.md` marker、`README.zh-TW.md` marker 與 `package.json` version 皆已對齊 v0.18.2 / 0.18.2
