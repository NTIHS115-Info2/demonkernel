# UpdateLog v0.17.0

## Metadata
- Category: main
- Scope: Main
- Version: 0.17.0
- Date: 2026-05-17
- Branch: main
- Commit: dbdf148c

## Summary
- 新增 system-prompt-manager system plugin，並讓 TalkEngine 在 LLM messages 第一筆加入 common system prompt

## Changes
### Added
- 新增 active ExecPlan，盤點 repository 是否已有系統提示詞管理機制並延伸為 v0.17.0 implementation plan
- 新增 `system-prompt-manager@0.1.0` system plugin，提供 `system.prompt.manager.get` capability
- 新增檔案式 `common` system prompt lookup；實際 prompt 內容檔不提交，需由本機或部署環境提供
- 新增 prompt manager focused tests 與 pluginsManager discovery/online/capability registration coverage

### Changed
- 將正式修改前的 system prompt management 探索歸入 v0.17.0 準備範圍
- TalkEngine 新增 `system-prompt-manager@0.1.0` exact dependency，並在 LLM messages 最前面加入 `common` system prompt

### Fixed
- 本版未修正既有 runtime bug

### Removed
- 本版未移除既有功能

## Impact
- 維護者可在本機或部署環境提供 `src/systemPlugins/system-prompt-manager/assets/prompts/common.system.prompt.md` 自訂 common system prompt；該內容檔預設不提交
- TalkEngine 後續 request 會帶有可由檔案自訂的 system prompt；本版不引入 prompt policy、validation 或版本治理

## Tests
- 通過：`yarn test tests/systemPlugins/system-prompt-manager.test.ts tests/systemPlugins/talk-engine.prompt-composer.test.ts tests/systemPlugins/talk-engine.test.ts`
- 通過：`yarn test tests/pluginsManager`
- 通過：`yarn build`
- 通過：`yarn lint`（既有 `tests/systemPlugins/discord.test.ts` unused variable warning，exit code 0）

## Risks & Rollback
- 若 prompt 檔缺失，本版會回傳 fallback prompt 並記 warning；回滾可移除 prompt-manager dependency 與新 plugin

## Notes
- 使用者指定 0.17.0 作為準備階段 target version
- 使用者確認 v0.1 manager 只作為 prompt provider，不做 prompt 驗證、安全 policy、版本治理或狀態判斷
- 使用者確認 prompt 內容檔不提交到 repository
