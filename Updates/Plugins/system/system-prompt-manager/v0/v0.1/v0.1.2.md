# UpdateLog v0.1.2

## Metadata
- Category: plugin
- Scope: system/system-prompt-manager
- Version: 0.1.2
- Date: 2026-05-18
- Branch: main
- Commit: dbdf148c

## Summary
- 將 default system prompt 改為可由檔案提供的 fallback，且 prompt 內容不提交

## Changes
### Added
- 新增 `assets/prompts/default.system.prompt.md` lookup path；實際內容檔由本機或部署環境提供
- 新增 default prompt fallback 與熱讀取測試

### Changed
- 缺少 requested state prompt 時改讀 `default.system.prompt.md`
- 缺少 state input 時改讀 `default.system.prompt.md`
- README 補充 default prompt 檔案位置與 fallback 行為

### Fixed
- 修正 default prompt 需要修改 code 才能調整的問題

### Removed
- 移除 hard-coded default prompt 作為主要 fallback 的行為

## Impact
- default prompt 與普通 prompt 使用相同的檔案 lookup 規則
- 運維或開發者可在本機或部署環境提供 default prompt 檔案，不需要改 TypeScript

## Tests
- 通過：`yarn test tests/systemPlugins/system-prompt-manager.test.ts tests/systemPlugins/talk-engine.prompt-composer.test.ts tests/systemPlugins/talk-engine.test.ts`
- 通過：`yarn test tests/pluginsManager`（logger cleanup stderr warning，exit code 0）
- 通過：`yarn build`
- 通過：`yarn lint`（既有 `tests/systemPlugins/discord.test.ts` unused variable warning，exit code 0）

## Risks & Rollback
- 若 default prompt 檔案未由本機或部署環境提供，會使用 emergency fallback；回滾可退回 `0.1.1`

## Notes
- emergency fallback 只用於 default prompt 檔案也不可用的最後防線；prompt 內容檔不提交到 repository
