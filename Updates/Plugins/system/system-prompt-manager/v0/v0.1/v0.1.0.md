# UpdateLog v0.1.0

## Metadata
- Category: plugin
- Scope: system/system-prompt-manager
- Version: 0.1.0
- Date: 2026-05-17
- Branch: main
- Commit: dbdf148c

## Summary
- 新增 system-prompt-manager 作為檔案式 system prompt provider

## Changes
### Added
- 新增 `system.prompt.manager.get` capability 與 `getSystemPrompt({ state: "common" })` provider API
- 新增 `assets/prompts/common.system.prompt.md` 作為 common system prompt lookup path；實際 prompt 內容檔不提交，需由本機或部署環境提供
- 新增 local strategy，每次呼叫都重新讀取 prompt 檔案

### Changed
- 無既有行為變更

### Fixed
- 本版未修正既有 runtime bug

### Removed
- 本版未移除既有功能

## Impact
- TalkEngine 可透過 capability 取得 system prompt，不需要在自身內部硬編提示詞
- 維護者可在本機或部署環境提供 prompt 檔案影響後續輸出

## Tests
- 通過：`yarn test tests/systemPlugins/system-prompt-manager.test.ts tests/systemPlugins/talk-engine.prompt-composer.test.ts tests/systemPlugins/talk-engine.test.ts`
- 通過：`yarn test tests/pluginsManager`
- 通過：`yarn build`
- 通過：`yarn lint`（既有 `tests/systemPlugins/discord.test.ts` unused variable warning，exit code 0）

## Risks & Rollback
- 若 prompt 檔缺失，provider 會回傳 fallback prompt 並記 warning
- 回滾可移除 `src/systemPlugins/system-prompt-manager` 與 TalkEngine dependency

## Notes
- v0.1 只提供 prompt string，不做 validation、policy、version governance 或狀態判斷；prompt 內容檔不提交到 repository
